http:
  routers:
    proxmox:
      rule: "Host(`proxmox.home.m1xxos.online`)"
      service: "proxmox"
      entryPoints:
      - "websecure"
      - "web"

  services:
    proxmox:
      loadBalancer:
        servers:
        - url: "https://192.168.1.134:8006"
        serversTransport: my-insecure-transport
