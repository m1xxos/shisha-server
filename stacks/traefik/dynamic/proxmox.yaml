http:
  routers:
    proxmox:
      rule: "Host(`proxmox.home.m1xxos.tech`)"
      service: "proxmox"
      entryPoints:
      - "websecure"
      - "web"

  services:
    proxmox:
      loadBalancer:
        servers:
        - url: "http://192.168.1.134:8006"
