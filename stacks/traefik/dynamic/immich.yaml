http:
  routers:
    immich:
      rule: "Host(`photos.m1xxos.online`) || Host(`photos.home.m1xxos.online`)"
      service: "immich"
      entryPoints:
      - "websecure"
      - "web"
      tls:
        certresolver: "based"

  services:
    immich:
      loadBalancer:
        servers:
        - url: "http://192.168.1.138:30041/"
