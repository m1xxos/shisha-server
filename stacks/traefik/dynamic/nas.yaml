http:
  routers:
    nas:
      rule: "Host(`nas.home.m1xxos.online`)"
      service: "nas"
      entryPoints:
      - "websecure"
      - "web"

  services:
    nas:
      loadBalancer:
        servers:
        - url: "http://192.168.1.138/"
