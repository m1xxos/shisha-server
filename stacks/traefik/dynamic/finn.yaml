http:
  routers:
    finn:
      rule: "Host(`finn.m1xxos.online`) || Host(`finn.home.m1xxos.online`)"
      service: "finn"
      entryPoints:
      - "websecure"
      - "web"
      tls:
        certresolver: "based"

  services:
    finn:
      loadBalancer:
        servers:
        - url: "http://192.168.1.138:8096/"
